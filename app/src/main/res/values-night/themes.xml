<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoTFG" parent="Theme.Material3.Light.NoActionBar">
        <!-- Colores de marca primarios para ambos modos. -->
        <item name="colorPrimary">@color/navy_green</item>
        <item name="colorPrimaryVariant">@color/willow_tree</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Colores de marca secundarios para ambos modos. -->
        <item name="colorSecondary">@color/barf_green</item>
        <item name="colorSecondaryVariant">@color/quinoline_yellow</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Color de la barra de estado para ambos modos. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Personaliza tu tema aquí. -->
    </style>

    <style name="DarkActionBar.Theme.ProyectoTFG" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Colores de marca primarios para ambos modos. -->
        <item name="colorPrimary">@color/navy_green</item>
        <item name="colorPrimaryVariant">@color/willow_tree</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Colores de marca secundarios para ambos modos. -->
        <item name="colorSecondary">@color/barf_green</item>
        <item name="colorSecondaryVariant">@color/quinoline_yellow</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Color de la barra de estado para ambos modos. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Personaliza tu tema aquí. -->
    </style>
</resources>